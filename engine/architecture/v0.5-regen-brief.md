# System Map v0.5 — Regeneration Brief for Claude Design

**Status:** Open work — diagram regeneration pending
**Resolves:** ADR-009 (regenerate system-map diagram to v0.5)
**Output target:** `engine/architecture/gtmstack-system-map-v0.5.png`

---

## Why this is needed

The current diagram (`gtmstack-system-map-v0.4.png`) labels the Activation
layer's website tile as **Framer**. As of May 2026 that's wrong — the site
was migrated to **Next.js + Vercel** (see commit `2ede62f` and the v0.5 entry
in [the system-map notes](./gtmstack.ai-engine-system-map-v0.2-notes.md)).

The notes file is up to date. The PNG is stale. We need to regenerate it.

---

## Brief — paste this into Claude Design

```
Regenerate the GTM Stack system map diagram to v0.5.

Source of truth for the architecture, layer breakdown, ownership labels,
and infrastructure table:
  https://github.com/Marketing-Matt/gtmstack/blob/main/engine/architecture/gtmstack.ai-engine-system-map-v0.2-notes.md

Source of truth for the brand (colours, type, visual language):
  https://github.com/Marketing-Matt/gtmstack/blob/main/brand/style-guide.md
  https://github.com/Marketing-Matt/gtmstack/blob/main/brand/tokens.json

Reference layout:
  https://github.com/Marketing-Matt/gtmstack/blob/main/engine/architecture/gtmstack-system-map-v0.4.png

Match the v0.4 layout, typography, and visual treatment exactly. Only the
content changes listed below should differ.

CHANGES FROM v0.4 → v0.5:

1. Activation layer
   Replace the "Framer" tile labelled "gtmstack.ai Website" with:
     Tool: Next.js + Vercel
     Note: "Code in site/. Auto-deploys on git push to main."

   Replace the "Framer" tile labelled "Skills Marketplace" with:
     Tool: Next.js + Vercel
     Note: "Same site/ codebase."

   Replace the "Beehiiv + Framer" tile labelled "Audience Capture" with:
     Tool: Beehiiv
     Note: "Subscribe-form embed in the Next.js site."

2. Infrastructure table (bottom-of-diagram block)
   Remove the "Framer" row entirely.
   Replace with TWO new rows:
     - Next.js + Vercel | Website + Skills Marketplace | ✅ Active
       Note: "git push → auto-deploy. Branch previews enabled."
     - Claude Code | Site authoring + edits | ✅ Active
       Note: "Code edited in Claude Code; commits flow direct to GitHub → Vercel."

3. Design output pipeline (if shown as a sub-diagram)
   The arrow currently labelled "Framer → site build" should now read:
     "Next.js (site/) → Vercel auto-deploy → gtmstack.ai"

4. Version label
   Header / corner version stamp: change "v0.4" to "v0.5".
   Date stamp: "May 2026".

5. Footer / sign-off
   Keep the existing "_> gtmstack.ai — Unfiltered AI marketing. Built live."

DO NOT CHANGE:
- The five-layer architecture (Data → Intelligence → Orchestration → Activation → Feedback)
- The feedback loop arrow returning to Data
- Ownership labels and risk callouts
- All other infrastructure tools (Claude, Beehiiv, ElevenLabs, Airtable, Canva, GitHub, Namecheap)
- Colour palette, typography, lockups — must remain identical to v0.4
- The terminal-chrome / markdown-native visual language

OUTPUT:
- File format: PNG, transparent or #0A0A0A background (match v0.4)
- Resolution: at least 2x for retina; matches v0.4 dimensions
- Filename: gtmstack-system-map-v0.5.png

Once generated, the file should be committed to engine/architecture/
alongside the v0.4 (which we keep for history).
```

---

## After Claude Design returns the PNG

1. Save it to `/Users/matthewbrowning/gtmstack/engine/architecture/gtmstack-system-map-v0.5.png`
2. Update `engine/architecture/README.md`:
   - Change current-system-map link from v0.4 to v0.5
   - Move v0.4 to a "previous versions" section (don't delete — keep history)
   - Close ADR-009 in the open-decisions table
3. Update `README.md` (root) line 102 if it still references v0.4
4. Commit and push:
   ```
   git add engine/architecture/
   git commit -m "Regenerate system map to v0.5 — Next.js + Vercel"
   git push origin main
   ```

---

*>_ gtmstack.ai — Unfiltered AI marketing. Built live.*
